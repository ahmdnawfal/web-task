export type TypeDataCustomers = {
  title: string;
  name: string;
  image: string;
};

export type TypeLinkNav = {
  name: string;
};

export type TypeDataServices = {
  title: string;
  cardTitle: string;
};

export type TypeTranslations = {
  [key: string]: string;
  home: string;
  services: string;
  customers: string;
  teams: string;
  contact: string;
};
